{
  "name": "bitmark-kernel-browser-node",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "A Bitmark (BTM) node + wallet in a browser tab — bitcoin-kernel/browser-node on a live coin.",
  "license": "AGPL-3.0-or-later",
  "devDependencies": {
    "webtorrent": "^2.8.5"
  },
  "dependencies": {
    "classic-level": "^3.0.0"
  }
}
